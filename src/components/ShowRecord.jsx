function ShowRecord({record}) {
    return(
        <div>
            {/* {
                record !== undefined ? record.map((data, index))
            } */}
        </div>
    )
}

export default ShowRecord();